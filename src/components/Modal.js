import styled from "styled-components"

const ModalMain = styled.div`
    height: 100%;
    width: 100%;
    position: absolute;
    background-color: rgb(245, 223, 77, 0.3);
`

const Modal = () => {
    return (
        <ModalMain>

        </ModalMain>
    )
}

export default Modal